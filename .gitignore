__pycache__/
*.pyc
.pytest_cache/
logs/
*.egg-info/
.env.local
.venv/
# Local-only (Termux) tooling — never pushed to GitHub:
# agent skills used while building (classifier.dev, ask-matt, tdd, ...),
# all agent-copy dirs from `skills add`, and the skills lock file.
.claude/
.agents/
agent/
skills/
skills-lock.json
.ruff_cache/
# Durable derived state (SQLite) lives under logs/ and should not be committed.
*.db
*.db-wal
*.db-shm
