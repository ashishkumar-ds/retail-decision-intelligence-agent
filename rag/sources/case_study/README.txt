This folder is reserved for vetted dunnhumby / 84.51° case-study narratives
(when a real, attributable vendor case study is curated and dropped here as
Markdown, corpus.build_chunks() tags it source_type=case_study).

It is intentionally EMPTY: the research pass found that dunnhumby vendor case
studies are marketing narratives (Tier-2 business context only), and none has
been curated into an ingest-ready, license-permissible Markdown source yet.
Including a made-up or unattributed "case study" here would violate the
project's fail-closed grounding policy.

This file is a plain-text note (.txt), not Markdown, so it is never ingested
into the corpus.